{
  "name": "cloudflare-rpc",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "cf-types": "wrangler types worker-configuration.d.ts --strict-vars=false",
    "typecheck": "pnpm run cf-types && tsc -p tsconfig.json --noEmit",
    "test": "pnpm run cf-types && vitest run",
    "check": "pnpm run cf-types && tsc -p tsconfig.json --noEmit && vitest run"
  },
  "dependencies": {
    "agents": "^0.7.9"
  },
  "devDependencies": {
    "@babel/core": "^7.29.0",
    "@babel/plugin-proposal-decorators": "^7.29.0",
    "@cloudflare/vitest-pool-workers": "^0.13.3",
    "@rolldown/plugin-babel": "^0.2.2",
    "@types/babel__core": "^7.20.5",
    "@types/node": "^25.5.0",
    "fast-check": "^4.6.0",
    "typescript": "^5.9.3",
    "vite-plus": "0.1.13",
    "vitest": "^4.1.0",
    "wrangler": "^4.76.0"
  }
}
